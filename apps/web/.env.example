# Origin of the ShortStory backend API.
# Leave blank to call the same origin (recommended in production — serve the PWA
# and the API behind one domain/reverse-proxy so /api/v1/* is same-origin and no
# CORS is needed). In dev, Vite proxies /api to this target.
VITE_API_URL=http://localhost:3000
